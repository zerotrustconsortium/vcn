version: '3.7'
services:
  vcn:
    image: golang:1.11.5-stretch
    entrypoint: "sh -c 'mkdir -p /tmp/bin && go get -v && go build -v && go test -v'"
    environment:
      GOPATH: /tmp
      GOBIN: /tmp/bin
    volumes: 
      - "./vcn:/go"
